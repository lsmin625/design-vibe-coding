OSS 패러디 디자인 플랫폼의 일반 고객은 다음과 같은 유즈케이스 목록을 포함한다.
프로젝트 지침에 따라 유즈케이스 단위로 순차적으로 처리한다.
먼저 회원가입 및 로그인 유즈케이스를 기반으로 스케치 코드를 작성하라. 

| 액터                | 주요 유즈케이스         | 세부 설명                       |
| ----------------- | ---------------- | --------------------------- |
| **A1. 일반 고객**     | ① 회원가입 및 로그인     | 이메일 또는 SNS 계정을 통한 가입·로그인    |
|                   | ② 상품 탐색 및 검색     | 카테고리, 키워드, 인기순/신상품 정렬 검색    |
|                   | ③ 디자인 상세보기       | 디자이너 정보, 제작 설명, 기부 비율 확인    |
|                   | ④ 상품 주문          | 옵션 선택(색상, 사이즈 등), 장바구니 및 결제 |
|                   | ⑤ 주문/배송 조회       | 주문 내역 및 배송 현황 실시간 조회        |
|                   | ⑥ 리뷰 및 SNS 공유    | 구매 후 상품 후기 작성 및 소셜 공유       |

---

## 회원가입 및 로그인

```yaml
component: SignupPage
description: >
  일반 고객이 OSS 패러디 굿즈 플랫폼에 신규 가입하는 화면.
  이메일/비밀번호 기반 가입을 우선 제공하고, 추후 SNS 계정 연동 버튼을
  하단 CTA로 노출한다.
layout:
  container:
    classes: "container d-flex align-items-center justify-content-center min-vh-100 bg-light"
    children:
      - type: "div"
        classes: "card shadow p-4 col-12 col-md-6 col-lg-4 bg-white"
        children:
          - type: "h1"
            classes: "h4 text-center mb-3 fw-bold"
            text: "회원가입"
          - type: "p"
            classes: "text-center text-muted mb-4 small"
            text: "오픈소스 팬 굿즈마켓에 오신 것을 환영합니다 🐧"
          - type: "form"
            attrs:
              @submit.prevent: "onSubmitSignup"
            children:
              # 이름
              - type: "div"
                classes: "form-floating mb-3"
                children:
                  - type: "input"
                    attrs:
                      type: "text"
                      id: "signupName"
                      placeholder: "이름"
                      class: "form-control"
                      v-model: "form.name"
                      :class: "{ 'is-invalid': errors.name }"
                    validation_note: "필수 입력. 2자 이상."
                  - type: "label"
                    attrs:
                      for: "signupName"
                    text: "이름"
                  - type: "div"
                    classes: "invalid-feedback"
                    v-if: "errors.name"
                    text: "{{ errors.name }}"
              # 이메일
              - type: "div"
                classes: "form-floating mb-3"
                children:
                  - type: "input"
                    attrs:
                      type: "email"
                      id: "signupEmail"
                      placeholder: "name@example.com"
                      class: "form-control"
                      v-model: "form.email"
                      :class: "{ 'is-invalid': errors.email }"
                    validation_note: "필수 입력. 이메일 형식 확인."
                  - type: "label"
                    attrs:
                      for: "signupEmail"
                    text: "이메일"
                  - type: "div"
                    classes: "invalid-feedback"
                    v-if: "errors.email"
                    text: "{{ errors.email }}"
              # 비밀번호
              - type: "div"
                classes: "form-floating mb-3"
                children:
                  - type: "input"
                    attrs:
                      type: "password"
                      id: "signupPassword"
                      placeholder: "비밀번호"
                      class: "form-control"
                      v-model: "form.password"
                      :class: "{ 'is-invalid': errors.password }"
                    validation_note: "8자 이상. 대/소문자+숫자 권장."
                  - type: "label"
                    attrs:
                      for: "signupPassword"
                    text: "비밀번호"
                  - type: "div"
                    classes: "invalid-feedback"
                    v-if: "errors.password"
                    text: "{{ errors.password }}"
              # 비밀번호 확인
              - type: "div"
                classes: "form-floating mb-3"
                children:
                  - type: "input"
                    attrs:
                      type: "password"
                      id: "signupPasswordConfirm"
                      placeholder: "비밀번호 확인"
                      class: "form-control"
                      v-model: "form.passwordConfirm"
                      :class: "{ 'is-invalid': errors.passwordConfirm }"
                    validation_note: "비밀번호와 동일해야 함."
                  - type: "label"
                    attrs:
                      for: "signupPasswordConfirm"
                    text: "비밀번호 확인"
                  - type: "div"
                    classes: "invalid-feedback"
                    v-if: "errors.passwordConfirm"
                    text: "{{ errors.passwordConfirm }}"
              # 생년월일
              - type: "div"
                classes: "form-floating mb-3"
                children:
                  - type: "input"
                    attrs:
                      type: "date"
                      id: "signupBirth"
                      placeholder: "생년월일"
                      class: "form-control"
                      v-model: "form.birthdate"
                      :class: "{ 'is-invalid': errors.birthdate }"
                    validation_note: "YYYY-MM-DD"
                  - type: "label"
                    attrs:
                      for: "signupBirth"
                    text: "생년월일"
                  - type: "div"
                    classes: "invalid-feedback"
                    v-if: "errors.birthdate"
                    text: "{{ errors.birthdate }}"
              # 전화번호
              - type: "div"
                classes: "form-floating mb-4"
                children:
                  - type: "input"
                    attrs:
                      type: "tel"
                      id: "signupPhone"
                      placeholder: "010-0000-0000"
                      class: "form-control"
                      v-model: "form.phone"
                      :class: "{ 'is-invalid': errors.phone }"
                    validation_note: "숫자/하이픈 포함 허용"
                  - type: "label"
                    attrs:
                      for: "signupPhone"
                    text: "전화번호"
                  - type: "div"
                    classes: "invalid-feedback"
                    v-if: "errors.phone"
                    text: "{{ errors.phone }}"
              # 약관 동의
              - type: "div"
                classes: "form-check mb-4"
                children:
                  - type: "input"
                    attrs:
                      class: "form-check-input"
                      type: "checkbox"
                      id: "tosAgree"
                      v-model: "form.agreeTos"
                      :class: "{ 'is-invalid': errors.agreeTos }"
                  - type: "label"
                    attrs:
                      class: "form-check-label small"
                      for: "tosAgree"
                    text: "서비스 이용약관 및 개인정보 처리방침에 동의합니다."
                  - type: "div"
                    classes: "invalid-feedback d-block"
                    v-if: "errors.agreeTos"
                    text: "{{ errors.agreeTos }}"
              # 가입 버튼
              - type: "button"
                classes: "btn btn-primary w-100 py-2 mb-3"
                attrs:
                  type: "submit"
                  :disabled: "loading"
                text: "회원가입"
              # 구분선
              - type: "div"
                classes: "text-center text-muted small mb-3"
                text: "또는"
              # SNS 로그인 영역 (확장 포인트)
              - type: "div"
                classes: "d-grid gap-2 mb-4"
                children:
                  - type: "button"
                    classes: "btn btn-outline-dark w-100"
                    attrs:
                      type: "button"
                      @click: "onSignupWithProvider('github')"
                    text: "GitHub 계정으로 시작하기"
                  - type: "button"
                    classes: "btn btn-outline-primary w-100"
                    attrs:
                      type: "button"
                      @click: "onSignupWithProvider('google')"
                    text: "Google 계정으로 시작하기"
              # 로그인 링크
              - type: "div"
                classes: "text-center"
                children:
                  - type: "span"
                    classes: "text-muted small me-1"
                    text: "이미 계정이 있으신가요?"
                  - type: "a"
                    classes: "small link-primary text-decoration-none"
                    attrs:
                      href: "/login"
                    text: "로그인"
state:
  # Vue <script setup>에서 ref/reactive로 선언될 상태
  form:
    name: ""
    email: ""
    password: ""
    passwordConfirm: ""
    birthdate: ""
    phone: ""
    agreeTos: false
  errors:
    name: null
    email: null
    password: null
    passwordConfirm: null
    birthdate: null
    phone: null
    agreeTos: null
  loading: false
methods:
  onSubmitSignup:
    description: >
      1. 클라이언트 단 유효성 검사 실행
      2. 에러가 없으면 loading=true
      3. 가입 API 호출 (POST /api/auth/signup)
      4. 성공 시 로그인 상태로 전환 또는 /login 이동
    emits:
      - "signupSuccess"   # 가입 성공 시 상위 라우터/스토어에 알림
      - "signupError"     # 실패 시 상위에서 토스트 등 처리
  validateForm:
    description: >
      각 필드별 필수 여부, 형식 검사(이메일 정규식, 비밀번호 일치 등)를 수행하고
      errors에 메시지를 세팅한다. 하나라도 오류가 있으면 false 반환.
  onSignupWithProvider:
    description: >
      GitHub/Google OAuth 플로우로 이동시키는 핸들러. 
      실제 구현은 window.location = '/oauth/...' 등으로 처리 예정.
props: []
emits:
  - signupSuccess
  - signupError
style:
  scoped: true
  notes:
    - "전역 색상은 Bootstrap 변수를 우선 사용. 커스텀 브랜드 컬러가 필요하면 여기서 .btn-primary 커스터마이징 가능 (프로젝트 공통)."
    - "카드 폭은 col-md-6 col-lg-4로 제한, 모바일에서는 col-12로 가득 차도록 해서 반응형 대응."


---

component: LoginPage
description: >
  기존 회원이 이메일/비밀번호 또는 SNS 계정으로 로그인하는 화면.
  로그인 성공 시 메인 피드(예: 추천 굿즈 리스트 페이지)로 이동.
layout:
  container:
    classes: "container d-flex align-items-center justify-content-center min-vh-100 bg-light"
    children:
      - type: "div"
        classes: "card shadow p-4 col-12 col-md-5 col-lg-4 bg-white"
        children:
          - type: "h1"
            classes: "h4 text-center mb-3 fw-bold"
            text: "로그인"
          - type: "p"
            classes: "text-center text-muted mb-4 small"
            text: "다시 오셨군요. 오늘의 신규 패러디 굿즈를 확인해 보세요 👀"
          - type: "form"
            attrs:
              @submit.prevent: "onSubmitLogin"
            children:
              # 이메일
              - type: "div"
                classes: "form-floating mb-3"
                children:
                  - type: "input"
                    attrs:
                      type: "email"
                      id: "loginEmail"
                      placeholder: "name@example.com"
                      class: "form-control"
                      v-model: "form.email"
                      :class: "{ 'is-invalid': errors.email }"
                  - type: "label"
                    attrs:
                      for: "loginEmail"
                    text: "이메일"
                  - type: "div"
                    classes: "invalid-feedback"
                    v-if: "errors.email"
                    text: "{{ errors.email }}"
              # 비밀번호
              - type: "div"
                classes: "form-floating mb-2"
                children:
                  - type: "input"
                    attrs:
                      type: "password"
                      id: "loginPassword"
                      placeholder: "비밀번호"
                      class: "form-control"
                      v-model: "form.password"
                      :class: "{ 'is-invalid': errors.password }"
                  - type: "label"
                    attrs:
                      for: "loginPassword"
                    text: "비밀번호"
                  - type: "div"
                    classes: "invalid-feedback"
                    v-if: "errors.password"
                    text: "{{ errors.password }}"
              # 비밀번호 찾기 / 회원가입 링크 row
              - type: "div"
                classes: "d-flex justify-content-between align-items-center mb-4 small"
                children:
                  - type: "a"
                    classes: "text-decoration-none link-secondary"
                    attrs:
                      href: "/forgot-password"
                    text: "비밀번호를 잊으셨나요?"
                  - type: "a"
                    classes: "text-decoration-none link-primary"
                    attrs:
                      href: "/signup"
                    text: "회원가입"
              # 로그인 버튼
              - type: "button"
                classes: "btn btn-primary w-100 py-2 mb-3"
                attrs:
                  type: "submit"
                  :disabled: "loading"
                text: "로그인"
              # 구분선
              - type: "div"
                classes: "text-center text-muted small mb-3"
                text: "또는"
              # SNS 로그인
              - type: "div"
                classes: "d-grid gap-2 mb-4"
                children:
                  - type: "button"
                    classes: "btn btn-outline-dark w-100"
                    attrs:
                      type: "button"
                      @click: "onLoginWithProvider('github')"
                    text: "GitHub로 로그인"
                  - type: "button"
                    classes: "btn btn-outline-primary w-100"
                    attrs:
                      type: "button"
                      @click: "onLoginWithProvider('google')"
                    text: "Google로 로그인"
              # 약간의 마케팅 메시지 (브랜드 톤)
              - type: "div"
                classes: "text-center text-muted small"
                text: "판매 수익 일부는 오픈소스 커뮤니티에 기부됩니다 🌱"
state:
  form:
    email: ""
    password: ""
  errors:
    email: null
    password: null
    auth: null   # 서버 측 인증 실패 메시지 (ex. '이메일 또는 비밀번호가 올바르지 않습니다')
  loading: false
methods:
  onSubmitLogin:
    description: >
      1. 기본 필드 유효성 검사 (빈 값 여부)
      2. loading=true
      3. 로그인 API 호출 (POST /api/auth/login)
      4. 성공 시 토큰/세션 저장 후 메인 페이지로 라우팅
      5. 실패 시 errors.auth에 서버 메시지 기록
    emits:
      - "loginSuccess"
      - "loginFailed"
  onLoginWithProvider:
    description: >
      GitHub/Google OAuth 로그인 진입.
      실제 구현은 window.location = '/oauth/...'
props: []
emits:
  - loginSuccess
  - loginFailed
style:
  scoped: true
  notes:
    - "form-floating 유지로 일관된 입력 스타일 제공"
    - "카드 폭은 col-md-5 col-lg-4 정도로 약간 더 넓게 주어 가독성 확보"
    - "오류 메시지(errors.auth)가 있을 경우 카드 상단이나 버튼 아래 alert로 노출 가능 (Bootstrap .alert-danger 사용 예정)"
security_considerations:
  password_handling:
    - "비밀번호는 절대 console.log 등으로 브라우저에 노출 금지"
    - "로그인 성공 시 access token은 httpOnly cookie 기반 세션을 기본 전략으로 고려"
  oauth:
    - "SNS 로그인 버튼은 실제로 OAuth redirect 트리거만 수행하고, 민감 스코프 최소화 안내 필요 (예: 이메일만 요청)"
navigation_after_success:
  signup:
    - "가입 성공 -> 자동 로그인 또는 LoginPage로 라우트"
  login:
    - "로그인 성공 -> '/home' 또는 추천 굿즈 피드 페이지로 라우트"

```

